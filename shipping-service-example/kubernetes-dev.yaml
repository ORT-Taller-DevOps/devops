apiVersion: apps/v1
kind: Deployment
metadata:
  name: "shipping-service-example"
  labels:
    app: "shipping-service-example"
    env: "dev"
spec:
  strategy:
    type: Recreate
  replicas: 2
  selector:
    matchLabels:
      app: "shipping-service-example"
      env: "dev"
  template:
    metadata:
      labels:
        app: "shipping-service-example"
        env: "dev"
    spec:
      nodeSelector:
        env: "dev"
      containers:
        - name: "shipping-service-example"
          image: "227416266472.dkr.ecr.us-east-1.amazonaws.com/shipping-service-example:latest-dev"
          imagePullPolicy: Always
          ports:
              - containerPort: 8082
                hostPort: 8082
          resources:
            requests:
              memory: 256Mi
              cpu: "0.5"
            limits:
              memory: 512Mi
              cpu: "1"
          readinessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 5
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /health
              port: 8082
            initialDelaySeconds: 30
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: "shipping-service-example"
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-path: /health
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-port: "8082"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-protocol: http
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-timeout: "60"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-interval: "120"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-unhealthy-threshold: "10"
    service.beta.kubernetes.io/aws-load-balancer-healthcheck-healthy-threshold: "2"
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: "shipping-service-example"
    env: "dev"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8082
