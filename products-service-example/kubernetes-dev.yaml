apiVersion: apps/v1
kind: Deployment
metadata:
  name: "products-service-example"
  labels:
    app: "products-service-example"
    env: "dev"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "products-service-example"
  template:
    metadata:
      labels:
        app: "products-service-example"
    spec:
      nodeSelector:
        env: "dev"
      containers:
        - name: "products-service-example"
          image: "894577096821.dkr.ecr.us-east-1.amazonaws.com/products-service-example:latest-dev"
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: 256Mi
              cpu: "0.5"
            limits:
              memory: 512Mi
              cpu: "1"
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 90
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: "products-service-example-service"
spec:
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: "products-service-example"
    env: "dev"
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080